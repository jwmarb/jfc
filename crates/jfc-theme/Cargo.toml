[package]
name = "jfc-theme"
version = "0.1.0"
edition = "2024"

[dependencies]
# Only needs ratatui's style types (Color, Modifier, Style) — no runtime.
ratatui = { version = "0.30", default-features = false }
